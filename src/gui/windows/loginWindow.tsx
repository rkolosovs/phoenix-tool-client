import * as React from "react";

export default class LoginWindow extends React.Component {
    render() {
        return (
            <div id={'loginWindow'}>
                <h1>Hello, Welcome to the test page separated into several files</h1>
            </div>
        );
    }
}